# 연구노트 — 2026.05.04

## 개요

큰 라운드. 계약서 evidentiary 강화 → Selected Products 공용화 → Cases 흐름 정리 → Agent home 전면 개편 → **Variants 모델 도입** → **Excel 일괄 업로드 (UPSERT + dry-run)** → 데이터 마스터 v12~v16 빌드.

5/8 마감 4일 전. 한 세션에서 처리한 양이 많아 항목별로 정리.

---

## 1. Contract Counter-sign 강화 (에이전트 온보딩)

### Approve & Activate 게이트
Admin이 NDA + Partnership 둘 다 카운터사인해야 "Approve & Activate" 버튼 활성화. 이전엔 agent가 사인만 하면 곧바로 승인 가능했음.

- `admin/agents/[id]/page.tsx` — 버튼 disabled 조건에 `every(c => c.admin_signed_at)` 추가, 안내 문구도 "Counter-sign every contract above before approving."로 강화
- Contract 리스트 행마다 `Counter-signed` (emerald) / `Needs sign` (amber) 배지로 시각적 시그널
- Agent 쪽 `/onboarding/waiting`에도 동일 배지 노출 + 15초 polling으로 admin 카운터사인 즉시 반영

### 서명 무결성 컬럼 신설
`agent_contracts`에 4개 컬럼 추가 — 위변조/본인확인 근거 강화.

```sql
ALTER TABLE agent_contracts
  ADD COLUMN IF NOT EXISTS signature_hash TEXT,
  ADD COLUMN IF NOT EXISTS signed_typed_name TEXT,
  ADD COLUMN IF NOT EXISTS admin_signature_hash TEXT,
  ADD COLUMN IF NOT EXISTS admin_signed_typed_name TEXT;
```

- 신규 sign API 2개 (`/api/onboarding/sign-contract`, `/api/admin/sign-contract`) — 서버에서 IP 추출, signature_data_url SHA-256 hash 계산, typed_name이 본인 이름과 매치하는지 검증
- ContractStep.tsx — typed name input + identity confirm 체크박스 + agree 문구 강화 (Korean Electronic Signatures Act 명시)
- Contract viewer (admin/agent 양쪽) — audit footer에 hash, IP, UA, typed name 노출. PDF print 시에도 출력물에 evidence metadata.
- Body token substitution 서버에서 처리 → 클라이언트가 body_snapshot 위변조 못 함

### Print PDF 레이아웃 fix
Admin/Agent contract viewer 둘 다 print 시 1페이지만 잘리던 이슈. 원인: 외부 layout이 `h-[100svh] overflow-hidden`로 viewport에 묶여있어서 `print:overflow-visible`이 효과 못 냄. Fix:

- `admin/layout.tsx`, `onboarding/layout.tsx` — print 시 `h-auto block` 오버라이드, sidebar/header `print:hidden`
- contract viewer outer flex 컨테이너에 `print:block print:h-auto` 추가
- 서명 박스 좌우 grid `print:grid-cols-2` (md 브레이크포인트가 print viewport에선 안 먹어서 강제), `print:break-inside-avoid`

### 기타 폴리시
- 사인 박스 사이즈 admin/agent 통일 (`h-32 w-full object-contain`)
- 한국어 "전자서명법" → "Article 3, general electronic signature" (UI 영어 규칙)

---

## 2. Selected Products 공용 컴포넌트

이전엔 admin은 split-pane 우측에 raw 표 + agent는 인라인 collapsible — 데이터 형식과 가격 계산까지 다름. 통합.

### 신규 컴포넌트 [`SelectedProductsSection.tsx`](src/components/SelectedProductsSection.tsx)
- `documents` prop으로 quotation + additional_invoice 모두 받음
- 그룹별 base_name + variant 라벨, USD primary + KRW secondary (admin), USD only (agent)
- `final_price` 직접 사용 (이전 agent는 `base_price × margin` 재계산했음 → admin이 finalize 후 가격 편집해도 agent 화면에 즉시 반영)
- Item 클릭 시 ProductDetailModal (description, location, partner, tag, 가격 등)
- Variant 묶기: 같은 base name + 다른 variant 라벨이면 한 row에 라벨로 분리

### Admin case page 단일 column 전환
50/50 split-pane 해제 → `max-w-3xl` single column. Selected Products는 Trip Setup 직후 위치(Agent와 동일 순서). Schedule placeholder도 Selected Products 다음에 항상 노출.

### Agent UI 정리
KRW 표시 제거 (`showKRW={false}` prop) — agent는 USD-only로 일관.

---

## 3. Cases 흐름 / Status 정리

### Awaiting Final Pricing 라벨
`Awaiting Pricing` → `Awaiting Final Pricing` ([caseStatus.ts](src/lib/caseStatus.ts)). vague했음 — finalize 단계임을 명시.

### Cases 리스트 DisplayGroup 누락 fix
[admin/cases/page.tsx](src/app/admin/cases/page.tsx)에 `awaiting_contract` / `awaiting_deposit` DisplayGroup이 빠져있어서 그 status의 case들이 silently dropped됨. 추가 + JUMP toolbar pill + 정렬 swap (Contract → Client Info 순서로).

### Estimated 가격 표시 (finalize 전)
finalize 전엔 `Total Amount` → `Estimated Quote`/`Estimated (KRW/USD)`로 라벨 변경 + 형광펜 효과 ("May change after admin finalizes"). `ESTIMATED` 배지 Financials 헤더 옆. Payment Due는 finalize 전 `Set when admin finalizes pricing` placeholder (날짜 노출 안 함).

### Trip Info amber 노이즈 정리
admin case detail에서 `awaiting_info` 외 단계에선 missing-info amber 경고 비노출 (`flagMissingInfo = caseData.status === 'awaiting_info'`). 이전엔 awaiting_contract 단계에서도 trip info 빠진 게 amber로 떠서 노이즈.

### Counter-sign / 3-party action 박스 indigo 통일
Hero "ACTION NEEDED" 박스와 인라인 3-Party Contract 박스가 amber/indigo 따로따로였음 → 같은 indigo로 통일. AdminCaseContractSection / AgentCaseContractSection 둘 다.

---

## 4. Agent Home 전면 개편

### 필터 좌측 → 상단 toolbar
좌측 사이드바 (w-52) 제거. 가로폭 product grid에 양보. 상단에 Search + 카테고리 pills + Muslim Friendly popover.

### Subcategory 필터 row 추가
parent 카테고리 선택 시 그 아래 subcategory pills row 등장 (가로 스크롤). All / DIAR / Dr.Tune's / ... 선택 시 그 sub만 필터.

### 카드 컴팩트화
- Image aspect-[4/3] → aspect-[16/10]
- Description 줄 제거 (detail 모달에서 확인)
- Price + duration 세로 stack (이전엔 가로 분배 다투다가 좁은 화면에서 "$"로 truncate되는 버그)
- 카드 상단에 subcategory chip ("HEALTH CHECK-UP", "DIAR" 등)으로 partner 식별

### 가격 range 표시 (multi-variant)
Single variant: `$583.56`. Multi-variant: `$5,836 – $23,342`. detail 모달에서 진짜 가격 노출.

### Section view 세로 스크롤 복구
이전엔 `md:h-full` + `min-h-0`로 4개 섹션이 viewport 안에 갇혀서 카드가 세로로 squash → image area 사라짐. 풀어서 자연 세로 스크롤. 모든 카드가 image placeholder 일관 노출.

---

## 5. ⭐ Product Variants 모델 도입 (큰 작업)

### 배경
같은 시술의 다른 회차/사이즈를 별도 product로 만드는 건 카탈로그 bloat. DIAR Membership Red/Purple/Black, MCT Stem Cell 1/3/5 sessions, Thermage 300/600/900 shots 같은 케이스. agent 카탈로그에선 1 카드만 보이고 디테일에서 variant 선택하는 게 자연스러움.

### 스키마 — `sql/2026-05-04_product_variants.sql`
```sql
CREATE TABLE product_variants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  product_id UUID NOT NULL REFERENCES products(id) ON DELETE CASCADE,
  variant_label TEXT,           -- NULL = 단일 variant (default)
  base_price NUMERIC NOT NULL,
  price_currency TEXT NOT NULL DEFAULT 'KRW',
  sort_order INT DEFAULT 0,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE document_items
  ADD COLUMN variant_id UUID REFERENCES product_variants(id) ON DELETE SET NULL,
  ADD COLUMN variant_label_snapshot TEXT;
```

`products.base_price`는 representative price로 보존 (compat). 신규 코드는 variants 테이블에서 읽음. 기존 documents는 default variant id로 backfill.

### Pricing 룰 — `lib/pricing.ts`
4/30 SOP 결정에 따른 카테고리별 마진 룰:
- **마진 적용**: K-Medical, K-Beauty, K-Wellness > **Spa만**, K-Starcation, K-Education
- **원가 그대로**: K-Wellness > Tour/Leisure/Shopping/K-Content, Subpackage 전체

```ts
export function appliesMargin(category, subcategory): boolean {
  if (category === 'K-Wellness') return subcategory === 'Spa'
  return ['K-Medical','K-Beauty','K-Education','K-Starcation'].includes(category)
}
export function variantPriceUsd({basePrice, priceCurrency, exchangeRate, marginMult, applyMargin}): number
export function variantPriceKrw(...): number
```

### Agent home cart 마이그레이션
- `Group.productIds: string[]` → `Group.items: { productId, variantId }[]`
- localStorage 마이그레이션 (옛 cart 형태면 자동 폐기)
- Single variant: + Group 1로 즉시 추가. Multi-variant: detail 모달에서 라디오로 선택 (variant마다 가격 + tap to add)

### Review page 마이그레이션
- 기존 `productIds.map → product.base_price × margin` 재계산 → `findVariant(productId, variantId) + variantPriceKrw` 룰 호출
- `addDocumentItem` 호출 시 `variantId` + `variantLabelSnapshot` 추가 (snapshot이 있으면 finalize 후 variant 삭제돼도 견적엔 라벨 보존)

### Quote/Invoice rendering
- `QuoteDocument.tsx` — line item description에 variant_label_snapshot 같이 표시 (`Thermage FLX — 600 shots`)
- `SelectedProductsSection.tsx` — variant_label snapshot을 product name 옆에 회색 prefix로

---

## 6. ⭐ Excel 일괄 업로드 (UPSERT + dry-run preview)

엑셀 마스터 파일을 admin이 직접 올려서 product/variant 일괄 등록·갱신.

### API — `/api/admin/products/upload-excel`
- multipart xlsx 받아서 SheetJS로 파싱
- Group by `(category, partner_name, name)` → product family. 같은 그룹 내 row들은 variant로 묶음
- **Match key**: `(category, partner_name, name)` natural key
- **차이 판별**: existing row와 desired row 비교 → 동일 skip / 다름 update / 신규 insert
- Variant: variant_label로 매칭, base_price/currency/sort_order/is_active diff
- **Subcategory**: 신규면 자동 생성 (`product_subcategories`에 INSERT)
- **Category**: 신규면 무시 (의도적 — 카테고리는 6개 fixed, Manage Categories에서 명시 추가만 허용)
- Solo 그룹 (variant 1개)은 variant_label NULL로 정리 (excel에서 잘못 들어간 라벨 자동 청소)

### Dry-run preview
`?dryRun=true` 쿼리로 호출 시 모든 write 스킵하고 카운트만 반환. Updated 항목은 field-level diff 함께.

### UI 플로우 (admin/products 페이지)
1. **Upload Excel** 버튼 (Add Product 옆, 같은 primary green) → 파일 선택
2. 자동 dry-run → **Review changes 모달** 노출:
   - Products / Variants 카운트 (신규/업데이트/그대로)
   - **Updated 표** (Type / Item / Field / Before / After) — sticky header, 같은 item의 여러 field는 rowspan으로 묶음
   - **Missing notice** — DB엔 있지만 이번 엑셀엔 없는 항목 카운트 + 펼침 list ("delete via product detail" 안내, 자동 삭제 안 함)
   - Errors / notices
3. **Confirm Save** / **Cancel**
4. Confirm 시 동일 파일로 두 번째 호출 (write 적용) → 결과 banner

### Safety
- 동일 파일 다시 올려도 unchanged로 카운트만 늘고 DB는 그대로
- 부분 업로드 (예: K-Beauty만) 해도 다른 카테고리 product 안 지움
- 의도적 삭제는 product detail 페이지에서

---

## 7. 데이터 마스터 v12 → v16 빌드

### v12 — 카테고리 K-prefix
- Medical → K-Medical, Beauty → K-Beauty, Wellness → K-Wellness 변경 (브랜드 일관성)
- K-Education, K-Starcation은 이미 prefix
- Subpackage는 그대로 (logistics/support 카테고리, K-prefix 안 어울림)
- Summary sheet 재계산 (이전엔 73 stale, 실제 125)

### v13 — 폴더 데이터 통합
[`scripts/build-master-v13.py`](scripts/build-master-v13.py):
- Ruby Clinic (K-Beauty) — 4 packages (mail에서 KRW 가격 + 구성품 풀)
- SOFITEL Ambassador Seoul (Subpackage Hotel) — 5 객실 + 3 패키지 (PDF 텍스트 추출)
- World K-POP Center "Special" — 4번째 K-Starcation tier (별도 견적)
- Dekabi placeholder (mail에 가격 정보 없어 TBD)
- DIAR PDF는 image 기반이라 이때 제외

### v14 — DIAR 159개 + Spa 19개 + 서브카테고리 재구성
[`scripts/build-master-v14.py`](scripts/build-master-v14.py):
- K-Beauty subcategory 룰 변경: 시술 타입 → 클리닉명 (DIAR / Dr.Tune's / Nest Clinic / Selena Clinic / Ruby / Dekabi)
- DIAR 159개 추가 (Membership / MCT Stem Cell / Lifting / Skin Booster / Filler / Botox 사이트별×브랜드별 / Body Filler / Signature Care / Premium Lifting / Stem Cell / Neck Care / Acne Pigment / Peeling / Hair Removal). 가격 만원 단위 → KRW 환산. VAT 별도 명시.
- Spa <100만원 19개 추가 (SPA the belec, SIGNIEL, Four Seasons SPA, Eco Jardin) — 이전에 1M 컷오프로 빠졌던 항목
- 총 317개 product

### v15 — variant_label 컬럼 + B열 정렬
- `variant_label` 컬럼 신설, 이름에서 sessions/shots/grades 패턴 추출 (128 row variant 추출됨)
- 카테고리 순 정렬 + sort_order 기준 + product_number 재발번 (1~317)
- 같은 base name + 다른 variant 라벨 → UI에서 (partner+name) 키로 묶어 카드 1장에 variant 선택 가능

### v16 — 데이터 품질 fix
[`scripts/build-master-v16.py`](scripts/build-master-v16.py):
- Dr.Tune's USD → KRW 환산 (v11에서 USD 값이 currency=USD로 잘못 들어갔던 것)
- Nest Clinic 5개 misnamed row (v11 Excel merged cell에서 name=None으로 들어왔던 것) → description 기반으로 정확한 이름 + variant_label 부여
- Nest Dermatological Procedures: 3-tier variants (4M / 6M / 8M)
- Nest Full Body Package: 3 protocols (Antiaging / Slimming / Slimming Total)

### 재import 후 결과
242 products / 317 variants. v16부터는 Excel upload UI로 일괄 갱신 가능 — 너가 직접 관리.

---

## 8. 기타 사용성 손질

### Agent setup wizard
- API에서 bank schema 키 정렬 (`account_holder` → `beneficiary`, `bank_address` → `address`) — UI는 이미 새 키 보내고 있었는데 API가 옛 키 기다려서 매번 400 났음
- 이메일 중복 pre-check (`auth.admin.listUsers()`로 해당 email 사용 user 있는지 → 있으면 친절한 에러 "already registered to another account") — 기존엔 Supabase 500 "Error updating user" opaque 메시지였음

### Case 생성 시 contract 자동 생성
[`agent/home/review/page.tsx`](src/app/agent/home/review/page.tsx) — 견적 만든 직후 `createCaseContract('three_party', ...)` 자동 호출. agent가 case 페이지에서 "Generate Contract" 버튼 따로 누를 일 제거. 실패해도 case 자체는 정상 — fallback 버튼 살아있음.

### Admin layout에 sidebar collapse + toolbar 정리
- Cases list `Awaiting Pricing` → `Awaiting Final Pricing` 섹션
- Admin products 표 컬럼 폭 조정 (Partner Name `max-w-[140px]` 2줄 wrap, Description `w-full`로 늘림, Price `min-w-[100px]`로 축소)

---

## 9. 코드 변경 요약

### 신규 파일
- `sql/2026-05-04_agent_contracts_signature_integrity.sql`
- `sql/2026-05-04_product_variants.sql`
- `src/app/api/onboarding/sign-contract/route.ts`
- `src/app/api/admin/sign-contract/route.ts`
- `src/app/api/admin/products/upload-excel/route.ts`
- `src/components/SelectedProductsSection.tsx`
- `src/lib/pricing.ts`
- `scripts/build-master-v13.py` ~ `v16.py`
- `scripts/import-master-to-db.py`

### 주요 수정
- `src/app/admin/agents/[id]/page.tsx` — Approve & Activate gate
- `src/app/admin/agents/[id]/contract/[contractId]/page.tsx` — typed name + audit footer
- `src/app/admin/cases/[id]/page.tsx` — single column, Selected Products inline, Estimated 표시, infoCollectionActive 뉴앙스, Schedule placeholder
- `src/app/admin/cases/page.tsx` — DisplayGroup 추가, 정렬 swap
- `src/app/admin/products/page.tsx` — Excel upload UI, 표 컬럼 폭, description line-clamp
- `src/app/admin/layout.tsx` — print 스타일 오버라이드
- `src/app/agent/home/page.tsx` — 필터 toolbar, subcategory pills, variants cart, 카드 컴팩트
- `src/app/agent/home/review/page.tsx` — variants 마이그레이션, contract 자동 생성
- `src/app/agent/cases/[id]/page.tsx` — Selected Products 공용 컴포넌트, 메모 정리
- `src/app/api/agent/setup/route.ts` — bank 키 정렬 + 친절한 에러
- `src/app/onboarding/contract/[contractId]/page.tsx` — Print 버튼, audit footer
- `src/app/onboarding/layout.tsx` — print 스타일
- `src/app/onboarding/waiting/page.tsx` — counter-sign 배지
- `src/components/AdminCaseContractSection.tsx`, `AgentCaseContractSection.tsx` — indigo 통일
- `src/components/ContractStep.tsx` — typed name + 강화된 agree
- `src/components/QuoteDocument.tsx` — variant_label_snapshot 표시
- `src/lib/caseStatus.ts` — 라벨 변경
- `src/lib/documents.ts` — variant_id field 지원
- `CLAUDE.md` — schema/policy 갱신
- `data/products_master_v12.xlsx` ~ `v16.xlsx`

---

## 10. Half-screen 잔손질 + 카드 사이즈 통일 + v17 데이터 (저녁 라운드)

### Admin /products 표 half-screen 대응
사이드바 접어도 Category/Price/Status 컬럼이 잘리는 문제. `<xl` (1280px 미만) 구간에 다음 적용:
- **Description 컬럼 숨김** (`hidden xl:table-cell`)
- **Price tier breakdown(15/20/25%) sub-section 숨김** (`hidden xl:block`) — base price만 노출
- 모든 컬럼 패딩 `px-6 → px-3`, 폰트 `text-xs → text-[11px]`, Price 메인 `text-base → text-xs`, Status badge 컴팩트화
- Product Name이 빈 자리를 채우도록 `xl:w-auto w-full`
- xl 이상에선 기존 디자인 그대로 (full-screen 영향 없음)

### Admin product 상세 description 입력창 높이
[ProductForm.tsx](src/components/admin/ProductForm.tsx) — `rows={4} resize-none` → `rows={10} resize-y`. 기본 높이 2.5배 + 사용자가 더 늘릴 수 있음.

### Agent home 카드 사이즈 통일
이전: All 보기(섹션) 카드 < 단일 카테고리 카드 — 섹션 뷰는 `lg:grid-cols-2 outer × md:grid-cols-3 inner = 6 effective cols`인데 단일 카테고리는 `lg:grid-cols-3 xl:grid-cols-4`로 더 큼. 단일 카테고리도 `grid-cols-2 md:grid-cols-3 lg:grid-cols-6`으로 맞춰서 lg+에서 동일 카드 폭.

### Agent 카드 partner_name 복귀
요청: subcategory chip("HEALTH CHECK-UP", "DIAR")만으론 K-Medical 같이 partner와 subcategory가 다른 카테고리에서 partner 식별이 어려움. subcategory 칩 아래 partner_name 한 줄 추가 (`text-[10px] text-gray-500 truncate`). 상세 모달 헤더에도 추가.

### 데이터 마스터 v17 — 1차 빌드
[`scripts/build-master-v17.py`](scripts/build-master-v17.py) — v16에서 패치:
- **Selection grade 괄호 33개 제거**: `(Silver)` 10개 / `(Gold)` 17개 / `(Diamond)` 6개 단독 토큰만. `(Diamond Premium)`, `(Suite)`, `(Face)`, `(full-face)` 같은 의미 있는 qualifier는 보존
- **K-Medical 20 rows의 gender suffix → variant_label**: `· Male`/`· Female` 제거하고 `variant_label="Male"/"Female"` 설정. Upload API가 (cat, partner, name) natural key로 그룹핑하므로 import 시 10 base + 20 variants로 변환됨
- **K-Medical description \n 복구**: Internal_Price `1. K-Medical` 시트에서 재추출. `*` / `•` bullet 줄바꿈 살아남 (9/10 base; SNUH Premium만 원본 자체가 한 줄)

⚠️ **Caveat → 해결**: 업로드해도 옛 row(`Premium (Diamond) · Male` 같은)는 missing-in-upload로 표시될 뿐 자동 삭제 X. 두 세트가 DB에 공존. 아래 "Upload-excel deleteMissing 모드"로 본질적 해결.

### Upload-excel `deleteMissing` 모드 추가
사용자 기대("엑셀에서 이름 바뀐 거 자동 반영")와 현실(natural-key 매칭이라 이름 변경 = insert + missing) gap을 메우기 위해 옵트인 삭제 토글.
- API ([upload-excel/route.ts](src/app/api/admin/products/upload-excel/route.ts)): `?deleteMissing=true` 쿼리 → missing 항목 일괄 DELETE. variants는 CASCADE, document_items.product_id는 SET NULL이라 case snapshot 무사. dry-run에서도 카운트 미리 반환.
- UI ([admin/products/page.tsx](src/app/admin/products/page.tsx)): missing 박스에 "Remove missing" 체크박스. 토글 시 dry-run 자동 재실행 (라이브 카운트), 박스 빨강 + 위험 메시지, Confirm 버튼이 빨강 "Confirm — Save & Delete N"으로 변신. 옵트인 디폴트 OFF.
- 검증 흐름: v17 업로드 → missing 33개(K-Medical 20 + K-Wellness 13) 떠야 정상 → 토글 ON → Confirm → 옛 row 다 사라지고 v17만 남음. 사용자가 직접 검증 완료, 마이그레이션 클린 클로즈.

### Admin /products 가격 컬럼 variant 인지화
agent home은 multi-variant `$5,836 – $23,342` 표시인데 admin은 base_price 단일이라 "성별마다, 회차마다 가격 어떻게 되는지" 확인 불가했음.
- products 쿼리에 `product_variants(...)` join 추가
- 메인 가격: variant 2+ 면 USD min/max range + "N variants" 보조 텍스트, 1개면 기존 base_price + USD 환산
- xl 이상 우측 컬럼: multi-variant면 **variant 한 줄씩 라벨/USD/KRW** (`Female  $1,759  ₩2,600,000`). single이면 기존 15/20/25% tier breakdown 유지
- 모바일 카드도 multi-variant range 적용

### Half-screen 추가 검토 (남은 부분)
사이드바 접어도 카드가 cramped한 다른 페이지가 있는지 모니터링.

### Agent 카탈로그 정렬
- 현재: products 자연 순서 (subcategory 안에서 sort_order)
- 옵션 1: 가격 desc (비싼 거 위) — 매출 친화적
- 옵션 2: 인기순 — `document_items` count로 5건+ 케이스부터 활성화
- 결정 보류

### Admin overview Revenue/Earnings
- 사용자 처음엔 "earnings 계산 이상"이라 했지만 본인 착각으로 정정 — 현재 markup-on-cost 방식 유지 (회사 50% margin = base × 0.5, 즉 revenue의 ~29%)

---

## 정성적 메모
- 한 세션에 너무 많은 작업이 몰려서 중간에 데이터 품질 이슈 (v15 variant 잘못 매칭 등) 발견하고 v16에서 fix하는 식의 iteration 발생. 다음엔 데이터 마스터 안정시키고 → 코드 → 테스트 순서로 가는 게 효율적일 듯
- Excel upload feature가 큰 가치 — 매번 사용자가 SQL 보내달라고 하지 않아도 됨
- Variants 모델은 DB import 시 grouping 룰이 중요. v16 → import script가 (cat, partner, name) natural key로 변환하면서 variants 자동 묶음
- 5/8 마감까지 남은 큰 작업: Excel 데이터 v17 정리 + agent 카탈로그 정렬 + UX 잔손질
- v17 패치 방식 한계 발견 → deleteMissing 토글로 해결. 처음 patch 빌드한 후 사용자가 "수정 자동 반영 아니야?" 지적 → API/UI 분석 → 옵트인 삭제 토글 추가. SQL로 일괄 삭제하지 않고 새 코드 검증을 위해 일부러 옛 row 유지하고 토글로 정리한 것이 깔끔. 앞으로 v18, v19 업데이트도 같은 흐름.
- Admin도 variant 인지화 안 되어있던 게 늦게 발견. agent home은 처음부터 range 표시였는데 admin /products는 base_price 단일이었음. variant 추가 후 admin 데이터 시야 동기화는 항상 같이 가야 함을 학습.

---

## 저녁 — 사용성 라운드 2 (variants UX + 감사 로그 sweep + 절반화면)

오후 라운드 끝나고 추가 피드백 한 번 더 일괄 처리. 큰 가치는 **variants UX 완성**(편집기 + 펼치기 + 2-level 드롭다운)과 **감사 로그 7개 surface 일괄 추가**.

### Agent 카탈로그 가격 desc 정렬
4/30 미팅 잔여 — "비싼 거 위로 노출이 매출 친화적". `priceSortKey(p)`: 활성 variant 중 최대 USD 가격(마진 룰 적용) 기준. 카테고리 선택 시엔 가격 desc만, section view(전체)에선 pinned(스냅샷 시점 카트 보유) 먼저 + 나머지 가격 desc. 인기순 정렬은 케이스 5건+ 쌓일 때 도입 예정 (backlog).

### DIAR + Hotel 데이터 컨솔리데이션 (v18 마스터)
**DIAR 159 rows → 11 base products + 159 variants**
사용자 요청: "DIAR Tension Lifting", "Skin Booster", "Filler", "Botox", "Body Filler", "DIAR Signature Care", "DIAR Private Programs", "Acne / Pigment / Pore / Peel", "Hair Removal — Face & Body", "MCT Stem Cell Therapy", "DIAR Membership"의 11개 묶음으로. product_number range로 매핑(P-021..P-179). variant_label = 시술명 + 스펙(`Thermage FLX · 300 shots`, `Square jaw · Allergan (USA)` 식). user 의견: Filler/Botox는 의도가 다른 상품군이라 분리 (33 variants 한 모달은 라디오 박살). Hair Removal은 동질적이라 한 묶음 OK.

**Hotel 37 rows → 13 base products + 37 variants**
파트너별 1 product, 룸 타입을 variant로. `[★5 Hotel]` suffix 전부 strip (모든 행에 붙어 있어서 정보가 아님), `[Hanok]` → `(Hanok)` 인라인 (정보 보존). grade paren strip(Deluxe/Suite/Special Suite/Standard/Premium/VIP Premium/VVIP)하되 `(Lake View)`, `(Minagi Omakase Set)` 같은 descriptive paren은 보존. INSPIRE의 `Sun Tower Inspire Suite` 중복은 `(#2)` suffix로 자동 분리.

빌드 스크립트 [`scripts/build-master-v18.py`](scripts/build-master-v18.py).

### Excel upload product_number 충돌 fix
사용자 보고: "v18 업로드해도 매번 +22 new가 뜸". 디버깅 결과 `count + 1` 기반 product_number 생성이 sparse한 번호 체계와 충돌. 예: count=109(unchanged) → 시도 #P-110부터, 그러나 v17 import에서 #P-001..#P-311이 점유 중 → INSERT failed (UNIQUE constraint), errors[]엔 쌓이지만 dry-run preview에선 보이지 않음 → 매번 같은 22개를 신규로 인식.

Fix: `MAX(numeric_part) + 1` 방식으로 변경 ([upload-excel/route.ts](src/app/api/admin/products/upload-excel/route.ts), [ProductForm.tsx](src/components/admin/ProductForm.tsx) 둘 다). Variants 도입 + DIAR 컨솔리데이션 같이 가는 마이그레이션에서 발견됐는데, 진작 발견됐으면 v17 업로드도 같은 영향이었을 것.

### Agent 상세 모달 — 2-level variant 드롭다운
요청: "Face / Face+Fusion / Scalp 만 먼저 보이고, 클릭하면 1·3·5 sessions 펼쳐지면 좋겠다". variant_label의 마지막 ` · ` separator로 split → prefix 그룹핑. 그룹 ≥2개 시 2-level 아코디언 (expand 시 sessions 노출), 그룹 1개거나 separator 없으면 flat 유지. 자식 1개 그룹은 inline 직접 추가, 자식 2개 이상은 expandable + 가격 range. 카트에 들어간 옵션 속한 그룹은 자동 펼침.

### Admin /products variants 풀 편집기 + 리스트 펼치기
"마감 생각하지 말고 장기적이면?" 질문에 **A 인라인 편집기 풀** 선택. 이유: Excel 라운드트립은 운영 중 자잘한 수정에 무거움 → UI는 일상 운영, Excel은 bulk seed 양립.

[ProductForm.tsx](src/components/admin/ProductForm.tsx):
- 단일 Price 입력 블록 제거 → Variants 섹션 신설
- 행마다: label / base_price / currency / Active 토글 / Delete + USD/KRW 환산 + 3-tier 마진 미리보기
- "Add Variant" 버튼
- Validate: ≥1 variant 필수, 가격>0, 라벨 unique, 다중일 때 빈 라벨 금지
- Save: 기존 variants UPDATE(id 매칭), 신규 INSERT, 제거 DELETE
- product.base_price/price_currency는 첫 variant 값으로 자동 sync (legacy 호환)
- 상세 페이지 [page.tsx](src/app/admin/products/[id]/page.tsx)에서 variants 로드 후 ProductForm initial로 전달

[admin/products/page.tsx](src/app/admin/products/page.tsx) 리스트 펼치기:
- "5 variants" 배지가 click-to-expand 토글로 동작
- 데스크탑: sub-row(3-col grid)에 라벨 / USD / KRW
- 모바일 카드: 카드 안 펼침
- 행 자체 클릭은 여전히 상세 진입 (stopPropagation)

### Admin /products 레이아웃 폴리시
사용자 한 손에 여러 피드백 — 한 항목씩 풀어가며 조정.
- **divider 위치 제멋대로**: `min-w-[140px]` (content-driven) → `w-[180px] flex-none` (fixed). 모든 행에서 divider 같은 x.
- **description 너무 좁다**: 처음 변경에서 variants 우측 컬럼 320px로 두고 `xl:min-w-[500px]`로 td 강제 → 회복하느라 320 → 240 → 200으로 단계적으로 줄임
- **Status 컬럼 제거**: 99% Active 라 정보량 X. 상품명 옆에 inactive일 때만 작은 회색 배지 인라인. ~80px 회수 → 일부는 variants(200→260), 일부는 description으로
- **Partner Name 컬럼 제거**: 모바일 카드 패턴 따라 상품명 아래 작은 회색 텍스트로 인라인. ~140px 회수. K-Beauty에선 subcategory==partner라 약간의 중복 있지만 다른 카테고리에선 명확히 의미 다름
- **헤더 절반화면 줄바꿈**: products/admin cases/agent cases/agent home 4곳. `flex-col md:flex-row` → `flex flex-wrap xl:flex-nowrap` 또는 항상 2-row(`flex flex-col gap-2.5`)로 단순화. agent home은 사용자 명세대로 1줄=Search+Muslim Friendly, 2줄=상위 카테고리, 3줄=하위 카테고리.

### Delete Product 버튼 박스 + USD 상품 KRW 환산
- ProductForm Delete: `text-red-500` plain → `border border-red-200` 추가로 박스화
- admin /products 가격: USD 단가 상품도 작은 회색 글씨로 `≈ ₩123,456` 환산. range도 마찬가지 (`≈ ₩72,900 – ₩1,370,250`).

### 감사 로그 — 7개 surface 일괄 + 모노톤 통일
사용자: "log에 엑셀로 상품 등록도 포함되어야 할 듯. 모든 활동이 다 로그에 저장되도록". 2단계로:
- **Step 1 (products)**: ProductForm save → `product.created`/`product.updated`, delete → `product.deleted`. Excel upload commit → `product.bulk_uploaded` (file name + 카운트 + deleteMissing). dryRun은 제외.
- **Step 2 (나머지)**: Categories(`category.*`/`subcategory.*`), system_settings(7개 키 모두 → `settings.updated`), contract templates(`contract_template.updated`), clients(create/update — 변경 필드 list), admins invite/delete, documents(issued/item_added/item_removed/paid).

audit page UI에 verb / tone / 아이콘 매핑 전부 추가. 기존 `agent.reassigned`, `case_contract.*`, `quote.finalized/repriced`, `partner.paid`, `agent_contract.countersigned`, `case.survey_submitted` 등 fallback dot 떴던 것들도 일괄 매핑.

**Step 3 — 아이콘 톤 monochrome 통일**: 기존 5톤(emerald/rose/blue/violet/amber) → 전부 `text-gray-500 + bg-gray-50 + ring-gray-200`. 4/29 폴리시(섹션 헤더 monochrome SVG) 라인 일관성. tone 필드는 코드에 남겨둠 (향후 확장 여지).

### Surveys 페이지 분리
사용자: "고객 리뷰 설정이 contracts 탭에 있는 게 맞나?" — 결이 다름(법적 문서 vs 후기). [/admin/surveys](src/app/admin/surveys/page.tsx) 신규 페이지: Questions 탭(질문 편집, super admin only) + Responses 탭(제출된 응답 리스트, case# / agent / 평균 별점 / 응답 상세). AdminSidebar에 메뉴 추가, contracts 페이지에서 survey 섹션 + state/handler/fetcher 전부 제거. survey 응답 모음 + 통계 향후 확장 여지 확보.

---

## v18 마스터 사용 흐름 정리
1. `py scripts/build-master-v18.py` → `data/products_master_v18.xlsx` 생성
2. admin /products → Upload Excel → v18 선택
3. dryRun=true, deleteMissing=true 로 preview — DIAR 159 + Hotel 37 = 196 missing 떠야 정상 (옛 row → 신 row 마이그레이션)
4. Confirm → 옛 row 삭제, 신 11+13 product 인서트, 변경분 196 variant 인서트
5. 처음 시도에서 product_number MAX+1 fix 적용 안 된 상태라 +22 new 반복 — fix 후 정상 동작
