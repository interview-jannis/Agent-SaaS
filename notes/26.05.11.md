# 연구노트 — 2026.05.11

## 개요

시뮬레이션 첫날(5/11) 직전 정리. 4건의 SOP/UX 조정 + 가격 환율 스냅샷 마이그레이션 안내.

1. 환율 스냅샷 컬럼 누락(`documents.price_rate_snapshot`) — 마이그레이션 안내
2. Deposit 플로우 단순화 — Agent → Client 발행 제거, Admin → Agent settlement만 유지
3. Agent Contact Info 필수화 (Phone/Country, Business Registration은 옵셔널 유지)
4. Group Assignment 정합성 — Shared Activities는 자동 적용 (인원 배정 불가)
5. 명칭 정리 — Shared → "Shared Activities", "Group" 용어는 인당 그룹 전용

---

## 1. `documents.price_rate_snapshot` 마이그레이션 안내

### 증상

```
Could not find the 'price_rate_snapshot' column of 'documents' in the schema cache
```

5/8 작업에서 추가한 `documents.price_rate_snapshot` 컬럼이 Supabase에 반영되지 않은 상태.

### 해결

`sql/2026-05-08_price_rate_snapshot.sql`을 Supabase SQL Editor에서 실행:

```sql
ALTER TABLE documents
  ADD COLUMN IF NOT EXISTS price_rate_snapshot NUMERIC;

INSERT INTO system_settings (key, value)
VALUES ('product_price_rate', '{"usd_krw": 1500}')
ON CONFLICT (key) DO NOTHING;
```

코드에는 `?? 1500` fallback이 있으므로 기존 문서는 자연스럽게 1500원으로 계산됨. 실행 후 PostgREST 스키마 캐시가 자동 reload되며, 안 풀리면 Dashboard → Settings → API → "Reload schema cache" 클릭.

### 부수 사항 — case_contracts realtime

`sql/2026-05-06_realtime_case_tables.sql`(cases / case_contracts / documents / schedules realtime 등록)도 미실행 가능성 있어 함께 점검 권장. 실행되어 있으면 계약서 사인 후 새로고침 없이 반영됨.

---

## 2. Deposit 플로우 단순화

### 배경

4/30 SOP에서는 deposit이 **두 단계**였음:
1. Agent → Client deposit invoice (client가 agent에게 50% 결제)
2. Admin → Agent deposit settlement (agent가 admin에게 forward)

운영 단순화를 위해 **agent → client 인보이스 발행 단계를 제거**. Agent는 client로부터 오프플랫폼으로 deposit을 받고, admin이 agent에게 deposit settlement만 발행. case status는 settlement leg 하나만으로 advance.

### 변경

**status 전이** ([caseTransitions.ts:61](src/lib/caseTransitions.ts:61))
- `isDepositPaid`는 admin → agent deposit_invoice의 `payment_received_at` 하나만 체크.

**UI** ([CaseDocumentsSection.tsx](src/components/CaseDocumentsSection.tsx))
- Agent의 "+ Deposit" 버튼 제거.
- `IssueIntent`에서 `deposit_to_client` 제거.
- Admin "+ Deposit Settlement" 버튼의 `!has.depositToClient` 선결조건 제거 → 계약 사인 직후 바로 발행 가능.
- 발행/`Mark Paid` 시 agent→client 분기 제거, admin→agent 분기만 유지.
- Agent용 안내 박스 추가(`awaiting_deposit`): quotation × deposit_percentage 추정 금액 + 발행 후엔 실제 settlement total 표시. "이 금액을 client한테 받아서 admin에게 전달하세요" 문구.

**Hero 액션** ([CaseHeroAction.tsx](src/components/CaseHeroAction.tsx))
- AgentCaseHero `awaiting_deposit`: subBits 3개 → 단순 "Forward deposit to admin" 헤드라인.
- AdminCaseHero `awaiting_deposit`: "Agent → Admin" 한 줄.
- Props에서 `depositInvoiceIssued` / `depositPaid` 제거.

**QuoteDocument** ([QuoteDocument.tsx:106](src/components/QuoteDocument.tsx:106))
- final_invoice의 deposit 차감 lookup이 admin→agent settlement 기준으로 변경. Client 뷰에서는 "이미 paid한 deposit = settlement 금액"으로 표시.

**lib helper**
- `issueDepositInvoice` 함수는 backward compat을 위해 남겨두되 호출처 모두 제거. dead code.

---

## 3. Agent Contact Info 필수화

### 결정

- **Phone / Country: 필수**
- **Business Registration: 옵셔널 유지** (외국 에이전트 대응)

### 변경

**Onboarding setup** ([onboarding/setup/page.tsx:73](src/app/onboarding/setup/page.tsx:73))
- `submit()` 검증에 `if (!form.phone.trim()) { setError('Phone number is required.'); return }` 추가
- 라벨 `Phone` → `Phone *`, "Optional" 문구 제거

**Profile contact** ([agent/profile/page.tsx:113](src/app/agent/profile/page.tsx:113))
- `saveContact()`에서 phone/country 빈 값 reject + 라벨에 `*` 표기
- 기존엔 `phone: phone.trim() || null`로 clear 가능했으나 `phone: phone.trim()`로 빈 저장 차단

---

## 4. Group Assignment 정합성 (Shared 자동 적용)

### 배경

`document_groups`에는 3종이 공존:
- **regular** — 개별 client/패키지 단위 (Group 1, Group 2, ...)
- **Shared Activities** — 케이스 전체 공통 활동 (통역·차량 등)
- **Trip Services** — Subpackage 상품 전용 (호텔, 식사 등)

Shared/Trip Services는 인원 배정 개념이 없는데도 기존 UI에서 멤버 배정 드롭다운에 노출되어 혼란 + 슬롯 카운터가 잘못 표시됨.

### 변경

**저장 단계** ([review/page.tsx:494](src/app/agent/product/review/page.tsx:494))
- Quote 생성 시 `group.id === 'shared'`이면 `addDocumentGroupMember` 스킵 → DB에 Shared용 `document_group_members` row 생성 안 함.

**Group Assignment UI** ([review/page.tsx:776](src/app/agent/product/review/page.tsx:776))
- Shared 카드는 chip/assign select 없이 "Applies to all members automatically — no individual assignment needed." 안내만.

**케이스 상세 멤버 편집** ([agent/cases/[id]/page.tsx](src/app/agent/cases/[id]/page.tsx))
- `assignableGroups` 도출(isAssignableGroup = name이 Shared/Shared Activities/Trip Services가 아닌 그룹).
- 멤버 편집 드롭다운 + Group slots overview + groupedMembers view 모두 `assignableGroups` 사용.

**완성도 계산기 (agent + admin + caseTransitions)**
- `expectedMemberCount` / `groupsComplete` / `groupGaps` / `setupCollapsed`의 `groupsOk` / save 시 `shortfalls` loop — 모두 Shared·Trip Services 제외.
- `caseTransitions.ts isInfoComplete` 동일 필터 적용 + SELECT 쿼리에 `name` 컬럼 추가.

### 영향 범위 파일

- [src/app/agent/product/page.tsx](src/app/agent/product/page.tsx) — cart의 Shared 라벨 "Shared Activities"로 변경, CART_VERSION 3→4 (기존 localStorage 무효화)
- [src/app/agent/product/review/page.tsx](src/app/agent/product/review/page.tsx)
- [src/app/agent/cases/[id]/page.tsx](src/app/agent/cases/[id]/page.tsx)
- [src/app/admin/cases/[id]/page.tsx](src/app/admin/cases/[id]/page.tsx)
- [src/lib/caseTransitions.ts](src/lib/caseTransitions.ts)

---

## 5. 명칭 정리 (Group vs Shared Activities)

### 배경

기존엔 모든 `document_group`을 UI에서 "Group N"으로 prefix(예: "Group 1: Shared · 5 pax"). Shared는 그룹 개념이 아니어서 어색했음. 또한 케이스의 "인당 그룹"과 헷갈림.

### 결정 (룰)

- **"Group N" 용어 = 인당 묶음(per-person grouping)에만 사용**
- Shared 콘텐츠 = "**Shared Activities**" (그룹 번호 없음)
- Subpackage 묶음 = "**Trip Services**" (그대로)

### 변경

**저장 명칭** — cart에서 'Shared' → 'Shared Activities' 3곳 ([agent/product/page.tsx](src/app/agent/product/page.tsx) lines 155 / 217 / 733). CART_VERSION bump.

**그룹 타입 판별** ([documents.ts:270](src/lib/documents.ts:270))
- `groupTypeFromName`이 legacy `'Shared'`와 신규 `'Shared Activities'` 둘 다 인식 (backward compat). 기존 DB row를 마이그레이트하지 않아도 동작.

**Quotation/Invoice 라벨** ([QuoteDocument.tsx:246](src/components/QuoteDocument.tsx:246))
- Shared Activities → `"Shared Activities · N pax"` (그룹 번호 없음)
- Trip Services → `"Trip Services"`
- 일반 그룹 → `"Group N · X pax"` 또는 `"Group N: customName · X pax"` (유지)

**ScheduleDocument 탭** — `g.name` 그대로 렌더 → 자동으로 "Shared Activities" 표시. 별도 수정 불필요.

**기타** — `admin/cases/[id]` 의 `isSharedGroup: grp.name === 'Shared'` 도 `'Shared Activities'` 추가 인식.

---

## 진행 영향

- 5/11~15 시뮬 시작. 위 5건 운영 중 발견될 추가 결함은 별도 노트로 누적.
- 컨텐츠 측 잔여(호텔 cutoff, WHY 컬럼 등)는 `notes/PROGRESS.md` "다음 할 일"에 그대로 남음.

---

## 시뮬 D+0 버그 수정 (5/11)

### 1. Schedule Editor — Shared 그룹 중복 표시

**증상**: 스케줄 아이템의 Group 드롭다운에 "Shared"가 두 개 표시됨.

**원인**: `caseGroups` prop을 `latestQuote.document_groups`에서 빌드할 때 `Trip Services`만 필터하고 `Shared` / `Shared Activities` 그룹은 포함시켰음. ItemRow 드롭다운에는 이미 하드코딩된 `<option value="">Shared</option>`이 있어 DB에서 온 Shared까지 이중 노출.

**수정** ([admin/cases/[id]/page.tsx:1685](src/app/admin/cases/[id]/page.tsx))
```js
// 기존
.filter(g => g.name !== 'Trip Services')
// 수정
.filter(g => g.name !== 'Trip Services' && g.name !== 'Shared' && g.name !== 'Shared Activities')
```

---

### 2. Edit Selected Products — 상품 클릭해도 추가 안 됨

**증상**: Edit Selected Products 상품 피커에서 상품을 클릭해도 staged add가 발생하지 않음.

**원인**: "Add to" 그룹 드롭다운이 `sortedGroups`(quotation document_group ID)로 옵션을 렌더하고 선택 값을 `pickerGroupId`에 저장. 하지만 `targetGroup = editGroups.find(g => g.id === pickerGroupId)`는 `editGroups`(final_invoice document_group ID)에서 찾기 때문에 UUID 미스매치 → `targetGroup === undefined` → `stageVariant` early return.

**수정** ([admin/cases/[id]/page.tsx:1289](src/app/admin/cases/[id]/page.tsx))
```js
// 기존 — sortedGroups(quotation) ID
{sortedGroups.length > 1 && <select value={pickerGroupId || sortedGroups[0].id} ...>
  {sortedGroups.map(...)}
// 수정 — editGroups(final_invoice) ID로 통일
{editGroups.length > 1 && <select value={pickerGroupId || editGroups[0]?.id} ...>
  {editGroups.map(...)}
```

---

### 3. ProductForm — 이미지 추가 버튼 누락

**증상**: 이미지를 1장 이상 추가하면 drop zone이 사라져서 추가 업로드 불가. 한 장씩만 올릴 수 있는 것처럼 보임.

**수정** ([ProductForm.tsx](src/components/admin/ProductForm.tsx)): 이미지 그리드 끝에 항상 `+` 타일을 렌더. 파일 input은 이미 `multiple` 속성이 있어 한 번에 여러 장 선택 가능.

---

### 4. GuideContent 이미지 업로드 RLS 차단

**증상**: Guide 화면에서 스크린샷/이미지 업로드 시 "업로드 실패: new row violates row-level security policy".

**원인**: `guide` 스토리지 버킷에 INSERT 정책이 없음. `product_images` 테이블 RLS나 `product-images` 버킷 정책은 관계없었음.

**해결** (Supabase SQL Editor):
```sql
CREATE POLICY "Allow authenticated uploads to guide"
ON storage.objects FOR INSERT TO authenticated
WITH CHECK (bucket_id = 'guide');

CREATE POLICY "Allow authenticated updates to guide"
ON storage.objects FOR UPDATE TO authenticated
USING (bucket_id = 'guide');

CREATE POLICY "Allow public read of guide"
ON storage.objects FOR SELECT TO public
USING (bucket_id = 'guide');
```
UPDATE 정책도 필요한 이유: `upsert: true`로 업로드하므로 파일 덮어쓰기 시 UPDATE 경로를 탐.

---

## 시뮬 D+0 오후 작업 (5/11)

### 5. #19 진행 상황 시각화 — 섹션 테두리 + 상태 뱃지

**목표**: 케이스 상세 화면에서 각 워크플로 섹션의 진행 상태가 명확히 보이도록.

**변경**:
- 모든 섹션 박스 `border` → `border-2` (1px → 2px), 색상 `gray-200` → `gray-300`
- Schedule Placeholder: 테두리 없던 것에 `border-2 border-gray-300` 추가
- Schedule History 헤더: 최신 버전 상태 뱃지 (Confirmed 녹색 / Pending Review 회색 / Revision Requested 빨강)
- Financials 헤더: Awaiting Payment(amber) / Paid(green) 뱃지 (Estimated는 유지)
- Partner Payouts 헤더: All Settled(녹색) / Pending(회색) 뱃지
- 컴포넌트 `AdminCaseContractSection`, `SelectedProductsSection`, `CaseDocumentsSection` 동일 적용

**영향 파일**: `admin/cases/[id]/page.tsx`, `AdminCaseContractSection.tsx`, `SelectedProductsSection.tsx`, `CaseDocumentsSection.tsx`

---

### 6. Balance Invoice 자동 발행 버그 수정

#### 6-1. 스케줄 재컨펌 시 이전 finalized invoice가 남는 문제

**증상**: 스케줄 revision → 재업로드 → 재컨펌 후에도 이전에 finalize된 `final_invoice.finalized_at`이 남아있어, admin이 Finalize Pricing 없이 balance invoice가 발행된 것처럼 보임.

**수정** ([agent/cases/[id]/page.tsx](src/app/agent/cases/[id]/page.tsx) `confirmSchedule()`):
```ts
// 스케줄 confirm 시 기존 final_invoice finalization 리셋
await supabase.from('documents')
  .update({ finalized_at: null, payment_due_date: null })
  .eq('case_id', caseData.id)
  .eq('type', 'final_invoice')
```
→ 스케줄 재컨펌할 때마다 admin이 항상 가격을 재확인해야 함.

#### 6-2. `createDraftFinalInvoice` 중복 생성 방지 (idempotent)

**증상**: 두 개의 auto-create 트리거(Edit Products + Finalize Pricing 섹션)가 동시에 실행되어 `final_invoice`가 중복 생성.

**수정** ([lib/documents.ts](src/lib/documents.ts)):
- `createDraftFinalInvoice` 호출 전 `final_invoice` 이미 존재하면 기존 것 반환, 신규 생성 안 함.

#### 6-3. `awaiting_pricing` 페이지 로드 시 auto-create 제거

**증상**: admin이 케이스를 열 때마다 `final_invoice`가 없으면 자동 생성 → 삭제해도 계속 재생성.

**수정** ([admin/cases/[id]/page.tsx](src/app/admin/cases/[id]/page.tsx)):
- 자동 생성 스피너 → **"Prepare Balance Invoice"** 명시적 버튼으로 교체
- admin이 직접 클릭해야만 draft final_invoice가 생성됨

#### 6-4. Agent 화면에서 미완성(draft) balance invoice 숨김

**증상**: admin이 Edit Products를 열면 draft final_invoice가 생성되고, agent 화면 INVOICES 섹션에 "Pending" 상태로 노출됨.

**수정** ([CaseDocumentsSection.tsx](src/components/CaseDocumentsSection.tsx)):
```ts
// final_invoice는 finalized_at이 있을 때만 표시
if (d.type === 'final_invoice') return !!d.finalized_at
```
→ Admin이 Finalize 완료 전까지 agent는 balance invoice를 볼 수 없음.

---

## 미해결 / 다음 확인 필요

- `최종 가격 입력 후 invoice가 자동 발행`되는 현상 추가 파악 필요 (UX 단계 분리 여부 결정 미완)
- Balance invoice를 가격 저장(draft)과 발행을 2단계로 분리할지 논의 필요

---

## 시뮬 D+0 저녁 작업 (5/11)

### 7. Schedule Editor — Row UX 전면 개선

**목표**: 저장된(committed) 아이템 행이 불필요하게 펼쳐져 있어 가독성 저하. 버튼 시인성 낮음.

#### 7-1. Details 접기/펴기

- Committed 행: Notes · Location · Internal note · Address · Partner contact · Driver info를 `▸ Details` 토글 뒤로 접음
- Transfer: From/To는 항상 노출 (핵심 요약), Transport mode · Label override는 Details 안
- Hotel: Check-in/out은 Row 2에 노출, Label override는 Details 안
- Meal: Meal type은 항상 노출, Restaurant · Cuisine은 Details 안
- Details 토글에 "N filled" 뱃지 (채워진 필드 수) 표시

#### 7-2. 행 레이아웃 재구성

- **Row 1** (type/block/time 행): committed 상태에서 select 드롭다운 → 컴팩트 텍스트 (`Transfer · Afternoon · 12:00 – 13:00`)로 표시. ↑↓ Edit ✕ 버튼 항상 오른쪽 끝 고정
- **Row 2**: FOR [그룹] + 호텔 Check-in/out + 상품 피커
- Edit 버튼: 아이콘만 → `✎ Edit` 텍스트 버튼으로 변경 (시인성 개선)
- ↑↓ 버튼 색: `text-gray-300` → `text-gray-500` (진하게)

**영향 파일**: [`src/components/admin/ScheduleEditor.tsx`](src/components/admin/ScheduleEditor.tsx)

---

### 8. Free time · Prayer — draft(pending) 상태로 시작

**증상**: `+ Free time` · `+ Prayer` 버튼 클릭 시 바로 committed 상태로 저장됨 → 시간 등 수정 불가.

**수정** (`addFreeTime()`, `addPrayerTime()`):
```ts
setItems(prev => [...prev, newItem])
setPendingItemIds(prev => { const n = new Set(prev); n.add(newItem.id); return n })
```
→ 추가 직후 draft 상태로 열려서 시간 입력 후 "Save row" 확정.

**영향 파일**: [`src/components/admin/ScheduleEditor.tsx`](src/components/admin/ScheduleEditor.tsx)

---

### 9. Schedule Item — 다중 그룹 배정 (Multi-group assignment)

**배경**: Transfer 같은 항목은 Mom + Dad는 동일하고 Kids만 다른 경우가 흔함. 기존 single-group select로는 표현 불가.

#### 9-1. 타입 변경

**[`src/types/schedule.ts`](src/types/schedule.ts)**:
```ts
groupId?: string | null    // legacy — backward compat용 유지
groupIds?: string[] | null // null = Shared(전체), string[] = 지정 그룹 목록
```
- `resolveGroupIds(item)` 헬퍼 추가: `groupIds` 우선, 없으면 `groupId` fallback

#### 9-2. GroupMultiSelect 컴포넌트

- 기존 single `<select>` → 체크박스 드롭다운으로 교체
- "Shared (all)" 라디오 + 그룹별 체크박스
- 레이블: "Shared" / "Mom" / "Mom + Dad" 등

#### 9-3. 왼쪽 스트라이프 색

- 그룹 1개: 기존 단색 4px
- 그룹 2개+: 해당 그룹 색을 위/아래 절반씩 표시 (`background-image: linear-gradient(to bottom, ...)`)
  - `border-image` 대신 background 방식 사용 (border-image는 divide-y 구분선에도 영향을 미치는 부작용)

#### 9-4. Coverage gate · committedVariantContexts

- 다중 그룹 아이템이 각 그룹 커버리지로 올바르게 집계되도록 수정

#### 9-5. ScheduleDocument 렌더러

- 탭 필터: `groupIds.includes(activeGroupId)` 로직으로 업데이트 (backward compat 포함)

**영향 파일**: `src/types/schedule.ts`, `src/components/admin/ScheduleEditor.tsx`, `src/components/ScheduleDocument.tsx`

---

## 시뮬 D+0 추가 수정 (5/11)

### 10. Agent 활성화 후 계약서 재열람 (`agent/profile/page.tsx`)

**문제:** Agent가 Setup Wizard 완료 후 자신이 서명한 계약서(NDA/Partnership)를 다시 볼 방법이 없었음.

**수정:** Profile 페이지에 "Signed Contracts" 섹션 신설.
- `agent_contracts` 테이블에서 본인 계약서 목록 조회
- 서명 일자, admin counter-sign 상태 표시
- "View" 링크 → `/onboarding/contract/{id}` 기존 뷰어 재사용

---

### 11. Subpackage Free → 호텔 0원 버그 수정 (`pricing.ts`)

**문제:** Subpackage 상품을 free(margin disabled)로 설정하면 같은 케이스의 호텔 가격도 0원이 됨.

**원인:** `subpackageMult()` 반환값(0)이 호텔 계산에도 곱해지는 연산 순서 오류.

**수정 (`src/lib/pricing.ts`):**
- `isHotelItem(item)` 체크 먼저 → 호텔은 `subpackageMult()` 우회, `nights × unitPrice`로만 계산
- `subpackageMult()`: config null이거나 `enabled=false` → 0 반환(무상), `enabled=true` → `1 + rate`

---

### 12. 케이스 목록 ↔ 상세 금액 환율 불일치 (`documents.ts` + cases list)

**문제:** Cases 목록의 총액과 케이스 상세 화면의 총액이 다르게 표시됨. 환율이 다르게 적용되고 있었음.

**원인:** 목록 쪽은 시스템 현재 환율로 KRW→USD 환산, 상세 쪽은 `documents.price_rate_snapshot`(발행 시점 스냅샷) 사용.

**수정:** Cases 목록 금액 계산도 `price_rate_snapshot` 기준으로 통일. 스냅샷 없는 구버전 문서는 1500원 fallback.

---

### 13. 고객 정보 Admin 인라인 패널 (`admin/cases/[id]/page.tsx`)

**문제:** Admin이 케이스 화면에서 고객 상세 정보를 확인하려면 별도 Clients 메뉴로 이동해야 했음.

**수정:** 케이스 상세에 "View client info" 토글 버튼 추가.
- `showClientPanel` state로 인라인 패널 show/hide
- 고객 정보(이름/국적/연락처/의료 정보 등) 케이스 페이지 내에서 바로 확인

---

### 14. Concierge Footer — Admin 이름 Fallback (`QuoteDocument.tsx`)

**문제:** Quotation/Invoice의 서명란 footer에서 agent 정보가 비어있으면 agent 이름이 공란으로 표시됨. 관리 목적상 admin 이름이 나와야 함.

**수정 (서명란 fallback 체인):**
1. `signer_snapshot` 이름
2. → `creatorAdmin.name` (발행한 admin)
3. → super admin 이름

admin 발행 문서는 admin 은행정보 + admin 이름, agent 발행 문서만 agent stamp + 은행정보 사용.

---

### 15. Financials 섹션 기본 열림 (`admin/cases/[id]/page.tsx`)

**문제:** Financials 섹션이 collapse 가능하게 구현되어 있어 매번 열어야 하는 불편함.

**수정:** Admin 케이스 상세의 Financials 섹션 `useState(false)` — collapse 불가, 항상 열린 상태 고정.
- Agent 케이스 상세는 terminal 상태(completed/canceled)에서만 collapse 버튼 노출 (기존 유지)
