# 연구노트 — 2026.05.06

## 개요

5/8 마감 D-2. 큰 라운드 — 시뮬레이션 직전 정합성 작업 + 운영성 개선.
주요 갈래: 3자 계약 evidentiary 강화 + 순서 무관 정책 + 실시간 자동 새로고침,
Cases 표 admin/agent 일관, Overview 캐시베이시스 회계, ScheduleEditor 대수술
(Free time 프리셋, partner eyebrow, block/time 범위, group 분리, internal note,
coverage gate), Edit Selected Products의 audit trail (Original/Added/Removed
영구 보존), agent → admin 메모(`agent_notes`).

---

## 1. Cases 표 admin/agent 통일 + 빈 상태 표 유지

Agent 쪽이 5/4 sweep에서 빠진 채로 있었음. admin 쪽엔 들어간 awaiting_contract /
awaiting_deposit DisplayGroup이 agent엔 누락 → 해당 status 케이스가 silently
드랍되어 안 보임. 6곳 (DisplayGroup type / DISPLAY_ORDER / DISPLAY_LABELS /
SHORT_LABELS / GROUP_ICON_PATHS / DISPLAY_STYLES) 모두 등록.

헤더 라벨 통일: `Amount (USD)` → `Total (USD)`, JUMP toolbar의 `Payment` →
`Balance`.

빈 상태에서도 표는 유지: 케이스 0건이어도 11개 빈 status 섹션 헤더가 보이고,
검색 결과 0건일 때도 동일. cases가 흐름을 따라가는 시각 도구라는 점에서, 0건
상태도 의미 있는 정보.

---

## 2. schedules.items 마이그레이션 누락 발견

5/5에 추가한 `sql/2026-05-05_schedules_items.sql`이 실제 Supabase에 적용 안 된
상태였음. 케이스 detail 페이지에서 `column schedules_1.items does not exist`
(PostgREST error 42703). SQL Editor에서 즉시 적용 후 정상.

교훈: SQL 마이그레이션 파일 추가 시 사용자에게 명시적으로 실행 지시.

---

## 3. 3자 계약 — 순서 무관 + Evidentiary 강화

### 순서 무관 (admin이 agent/client 사인 전에도 사인 가능)

이전 게이트: `agent_signed_at && client_signed_at && !admin_signed_at`. 운영
관점에서 admin이 chase 입장이라 굳이 마지막에 잠재적 병목으로 둘 이유 없음.
법적 효력은 세 사인이 모이면 발생, 순서는 무관.

[AdminCaseContractSection.tsx](src/components/AdminCaseContractSection.tsx):
- `canAdminSign = !contract.admin_signed_at`만 검사
- 카피 분기:
  - 셋 다 사인: "Fully signed."
  - admin만 사인: "Awaiting agent/client signature."
  - admin 미사인: "Counter-sign below — remaining parties can sign in any order."
- Status 전이(`awaiting_contract` → `awaiting_deposit`)는 `isFullySigned`
  (세 사인 모두) 기준이라 그대로 작동.
- Admin이 사인하면 agent에게 알림 (이전엔 `tryAdvanceContractSigned`가 full sign
  시에만 알렸는데, admin이 먼저 사인하는 케이스에선 agent가 모르고 지나가는 경우
  방지)

### Evidentiary 강화 (NDA/Partnership 5/4 패턴 mirror)

3자 계약은 deposit 50% 트리거하는 돈 흐름 직결 문서인데, NDA보다 evidentiary
등급이 낮았음 (직접 DB write, hash/IP/UA/typed_name 검증 없음). 결이 안 맞아
NDA 강화(5/4) 작업의 mirror.

**SQL** [`sql/2026-05-06_case_contracts_evidence.sql`]:
12개 컬럼 추가 — `case_contracts`에 agent/client/admin 각각 4개씩
(`signature_hash`, `signed_typed_name`, `ip_address`, `user_agent`).

**API 신규** (모두 service role + IP/UA + SHA-256 + body token 서버 substitute):
- `/api/case-contracts/sign-agent` — agent 세션 검증 + typed_name vs agents.name
  + 해당 case 소유 검증
- `/api/case-contracts/sign-client` — client_token이 auth, typed_name 자유 입력
- `/api/case-contracts/sign-admin` — admin 세션 검증 + typed_name vs admins.name

**UI** — [CaseContractViewer](src/components/CaseContractViewer.tsx) NDA
ContractStep 패턴 복사:
- "Read in full" 동의 + "Identity confirm" 체크 + typed name 입력 + 서명 패드 +
  KSA 명시 카피
- 모든 모드(agent/client/admin)에 동일 UX
- 모드별 컬러 강조 (agent=emerald, client=sky, admin=indigo) + "A"/"C"/"Co"
  원형 뱃지 + 부연 카피
- key prop으로 mode 전환 시 SignaturePad/typed name 강제 reset (이전 모드의
  서명 캔버스 잔존 이슈 fix)

**라이브러리 정리** [lib/caseContracts.ts](src/lib/caseContracts.ts):
- `signAsAgent/Client/Admin` 헬퍼 제거 (직접 DB 쓰기 → 서버 API 경유)
- `CaseContractRow` 타입에 evidence 컬럼 추가

### Save PDF 버튼

`/case-contract/{token}?print=1` 모드 추가 — sign UI 감추고 `window.print()`
자동 트리거. agent/admin 양쪽 contract 섹션에 `⤓ Save PDF` 버튼 (새 탭 오픈).
공개 client 페이지가 사이드바 없는 standalone이라 인쇄 결과 깔끔. 3열 sig
grid도 `print:grid-cols-3 print:break-inside-avoid` + 본문 max-h overflow
print: override.

---

## 4. 자동 새로고침 (Realtime)

3자 계약을 admin이 사인했을 때, agent 페이지 새로고침 안 하면 모름. 더 일반적
으로 케이스 detail 양쪽을 봐도 상태가 동기화 안 됨. Supabase Realtime 활용해
auto-refetch.

**SQL** [`sql/2026-05-06_realtime_case_tables.sql`]:
`supabase_realtime` publication에 `cases`, `case_contracts`, `documents`,
`schedules` 추가 (이전엔 `notifications`만).

**훅** [useCaseRealtime](src/hooks/useCaseRealtime.ts):
- caseId + onChange 받아서 4개 테이블의 변경을 단일 채널로 구독
- 250ms 디바운스
- unique instance id (page-level + child contract section 동일 caseId 충돌
  방지 — Supabase는 같은 채널명 두 번 subscribe 거부)

**적용**:
- agent/admin 케이스 detail 페이지: `useCaseRealtime(id, fetchCase)`
- AgentCaseContractSection / AdminCaseContractSection (자체 contract 상태
  fetch하니까) — 직접 `useCaseRealtime(caseId, load)` 구독

이제 두 브라우저 동시에 열어 두면 한쪽이 사인 → 다른 쪽 자동 갱신.

---

## 5. Overview 캐시베이시스 회계 + Partner Payouts 게이트

### Partner Payouts 활성화 시점

이전: `status === 'awaiting_travel' / 'awaiting_review' / 'completed'` (잔금
받은 후). admin이 한참 partner 비용 선결제 부담.

신규: schedule confirmed 시점부터 활성화 — 일정이 확정돼야 partner 항목별
지급 액수가 안정적이라는 운영 관점 (`caseData.schedules.some(s => s.status ===
'confirmed')`). 안내 카피: `Partner payouts unlock once the schedule is
confirmed by the agent.`

(중간에 `deposit_settlement received` 시점으로 한 번 갔다가, 다시 schedule
confirmed로 변경됐음.)

### Overview Revenue/Earnings — 캐시베이시스로 재정의

이전 모델 — accrual: revenue = quotation.total at cases.payment_date,
earnings = company margin formula로 분해.

신규 모델 — 캐시베이시스:
- **Revenue events**:
  (a) deposit_settlement(`type='deposit_invoice', to_party='agent'`).payment_received_at
      시점에 deposit 금액 인식
  (b) cases.payment_date 시점에 (총액 − 이미 받은 deposit) [잔금 proxy]
- **Costs**: partner_payments + settlements (이미 cash basis)
- **Earnings = Revenue − Costs** (단순 차감)

User 말로 정리: "파트너사 정산 전까지는 revenue와 earning 금액이 동일하다가,
파트너사 정산이 되면 revenue 금액은 그대로, partner costs는 늘어나고, 그만큼
earning에서 줄어야겠지". 정확히 이 모델.

[admin/overview/page.tsx](src/app/admin/overview/page.tsx):
- 신규 쿼리: deposit_settlement 받은 row 전체
- depositReceivedByCase Map으로 케이스별 받은 액 추적
- revenueEvents 빌드 (deposit 시점 + payment_date 시점)
- 월별 sparkline도 동일 모델 — 이벤트 시점 기준 집계
- Hero 서브카피 수정

**관련 admin 케이스 페이지 변경**:
- Financials 박스도 admin 쪽에 cyan tone 분기 추가 (`awaiting_deposit` /
  `awaiting_payment`) — 이전엔 agent에만 있어서 비대칭이었음

---

## 6. Documents — Selected Products audit trail

### origin + soft delete

[`sql/2026-05-06_document_items_origin_softdelete.sql`]:
- `origin TEXT NOT NULL DEFAULT 'original'` (`original` | `admin_added`) +
  CHECK constraint
- `removed_at TIMESTAMPTZ` (soft delete)
- 부분 인덱스 `(document_id) WHERE removed_at IS NULL`

[lib/documents.ts](src/lib/documents.ts):
- `addDocumentItem(input.origin)` 파라미터 추가
- `removeDocumentItem` → soft delete (UPDATE removed_at 대신 DELETE)
- `getDocumentItems`는 active만 반환
- `activeItems<T>` 헬퍼 (consumer가 client-side 필터)

**Sweep — `removed_at IS NULL` 필터 추가**:
- QuoteDocument (customer-facing render)
- SelectedProductsSection (case body)
- CaseDocumentsSection (invoices)
- agent/admin case detail page (multiple iteration sites)
- admin/cases list (partner status)
- admin/settlement (partner suggested 계산)
- schedule/[slug] (호텔 cover 추출)

Edit Selected Products는 의도적으로 미필터 — 모든 row(removed 포함) 노출이
audit trail이라는 디자인.

### Edit Selected Products UI 4-tier

| 상태 | Badge | 색 | 비고 |
|---|---|---|---|
| Persisted active, origin=original | `Original` (gray) | 일반 | |
| Persisted active, origin=admin_added | `Added` (emerald) | 일반 | |
| Persisted removed, origin=original | `Removed` (red) | line-through, 액션 X | 영구 |
| Persisted removed, origin=admin_added | `Removed Added` (red) | line-through, 액션 X | 영구 |
| Staged removal (이번 세션) | `Removing` (red) | line-through + Undo | 저장 시 persisted-removed로 |
| Staged add (이번 세션) | `New` (emerald) | 새 row | 저장 시 active-admin_added로 |

**그룹별 섹션 + 시각 분리**:
- `sortedGroups.map`으로 그룹 단위 sub-section
- 그룹 헤더 + `border-t-4` divider
- 그룹 안에서 정렬: 활성(Original→Added) → New 스테이징 → Removing/Removed (감사
  기록은 하단)
- 각 row 가격 2줄 (KRW primary + USD secondary, gray)
- partner_name + variant_label_snapshot 메타 row

**Picker 강화**:
- 모든 상품 노출 (slice cap 제거, 드롭다운 자체 스크롤)
- 카테고리 / 서브카테고리 셀렉트로 좁히기 (admin/products fetch에 product_categories
  + product_subcategories join 추가)
- 카테고리 정렬: `product_categories.sort_order` 기준
- 다중 variants 상품: `N variants ▼` 배지 + inline 펼침 → variant 라디오 선택
- 단일 variant 상품: 클릭 즉시 staging
- 같은 그룹에 staged-add된 product는 picker에서 제외 (중복 방지)
- removed_at 항목은 중복 set에서 제외 (실수로 삭제했던 product 다시 추가 가능)

**Original 항목 삭제 시 confirm 다이얼로그** — 고객 견적에 박힌 라인이라 명시
경고. admin_added는 confirm 없음 (자기 한 일이라).

**SaveChanges 보강**:
- removeDocumentItem들 → addDocumentItem들 → recalcDocumentTotal 호출
- document.total_price와 active 항목 합 일치 보장

### 카테고리 sort_order 정의

[`sql/2026-05-06_category_sort_order.sql`]:
K-Medical, K-Beauty, K-Wellness, K-Starcation, K-Education, Subpackage 순.
Agent home은 이미 sort_order 기준 fetch라 자동 반영. Admin Edit Selected
Products picker도 카테고리 옵션 정렬에 사용.

---

## 7. CaseDocumentsSection — 통화 + UX 정리

- Issue Deposit Invoice 모달의 ≈ 미리보기:
  - agent: `≈ $X` (USD only)
  - admin: `≈ $X (₩Y)` (USD primary + KRW context)
- 인보이스 카드 우측 상단 가격: agent USD only, admin USD + KRW 2줄
- **deposit_invoice 라인 아이템 비노출** — 단일 금액인데 라인 아이템으로 같은
  금액 또 뜨던 중복 제거
- 하단 액션 버튼 톤 통일: Preview / Copy link / Mark paid를 border + hover
  bg-white 형식으로 (다른 섹션의 Save PDF / Expand 버튼과 결 통일)

QuoteDocument의 `Independent Agent` 보조 라벨 제거 — From 라인엔 agent 이름만.

---

## 8. ScheduleEditor 대수술

### Free time 드롭다운

Day 카드 헤더의 "+ Add Item" 옆에 `+ Free time ▾`:
- Morning free / Afternoon free / Evening free / Full day free
- Full day는 morning+afternoon+evening 3 row 일괄 추가
- 모두 title="Free time" 자동 설정 → 저장 검증 통과

### Add Day / Remove Day 제거

여행 기간이 travel dates에서 파생되니 schedule editor에서 day 추가/삭제할
의미가 없음 (불일치 발생). 제거하고 카피 정리.

### 데이터 모델 확장 (JSONB라 SQL X)

[src/types/schedule.ts](src/types/schedule.ts) ScheduleItem:
```ts
endBlock?: ScheduleItemBlock | null  // null = 단일 block
endTime?: string | null              // 종료 시각
partner?: string | null              // partner brand (eyebrow)
internalNotes?: string | null        // 콘시어지 전용
groupId?: string | null              // document_groups.id; null = Shared
```

### Block / Time 범위

블록 → end block (Same/Morning/Afternoon/Evening) → 시작 시각 → 종료 시각.
ScheduleDocument에서 `09:00 – 15:00 · Morning → Afternoon` 같이 범위 표현.
사용자가 "정확한 시간 모를 수도" 지적 — block 범위만으로도 의미 전달 가능.

### Time24Input — locale-independent 24h

native `<input type="time">`이 OS/브라우저 locale에 따라 12h AM/PM으로 뜨는
이슈 발견 (en-US locale). HTML cross-browser로 24h 강제 옵션 없음.
`<select>` 두 개 (HH/MM)도 이전 라운드에서 작은 드롭다운 스크롤 빠지는 문제로
거부됐음. 결국 `<input type="number">` 두 개로 안착:
- HH (0-23) + MM (0-59), inputMode=numeric
- 타이핑 / 화살표 키 / 호버 스피너로 입력
- WebKit 스피너 CSS 숨김
- onBlur 시 0-padding + clamp

DateTime24Picker (flight 시간) + ScheduleEditor 양쪽에 적용.

### Partner eyebrow 분리

`Gil Hospital · VIP Premium · Female` 한 줄에 다 박힌 title이 시각적으로
부담. partner를 분리 필드로 빼고 작은 uppercase 라벨로 노출:
```
GIL HOSPITAL    (작은 회색 eyebrow)
VIP Premium · Female  (큰 serif title)
```
applyVariantPick은 `partner = cp.partnerName`, `title = ProductName · Variant`
로 분리 저장. deselect 시 admin이 손대지 않은 자동 채움 값만 clear (free-form
text는 보존).

### Internal note (admin only)

"콘시어지 핸드오버" 정보(드라이버, 컨택트, prep)는 운영용. 이전엔 `notes`
하나에 다 박혔는데, VIP 출력에 보이면 부적절. 신규 `internalNotes` 필드:
- Editor: gray-50 dashed-border input "Internal note (driver, contact, prep…)"
- Location도 internal로 demote (풀 주소는 VIP 입장에서 노이즈, 운영용)
- ScheduleDocument: `showInternalNotes=true` 모드에서만 amber 박스로 노출
- /schedule/[slug]?internal=1 쿼리로 admin preview만 internal 보임
- Admin preview 링크에 `internal=1` 자동 부착 (Schedule History "Preview"
  + Build Schedule 헤더 "Preview last version" 둘 다)
- Agent의 Copy/Send 링크는 그대로 (no internal)

### Confirmed 후 internal note 인라인 편집

스케줄 컨펌된 이후에도 콘시어지 정보(드라이버, 컨택트)는 운영 단계에서 변경
가능성 ↑. title/time/block 다 잠그되 internalNotes만 in-place 편집:
- Schedule History의 latest+confirmed 카드에 "Internal Notes (concierge
  handover)" 섹션
- Edit 버튼 → 인라인 편집 (각 row title 표시 read-only, internalNotes input만)
- Save: schedules.items JSONB UPDATE in-place (새 version 생성 X — 컨펌 사실
  자체는 변하지 않음)
- audit log `schedule.internal_notes_updated`

### Group 분리 (단일 schedule + URL 필터)

각 그룹(Female/Male)이 다른 시술 받는 케이스 대응. 옵션 A(그룹별 별도 schedule)
아니라 옵션 B 채택: 단일 schedule + 항목별 group 태그 + URL 필터.

- ScheduleItem.groupId = document_groups.id (null = Shared)
- ScheduleEditor row에 Group 셀렉트 (caseGroups.length > 1일 때만 노출)
- ScheduleDocument.filterGroupId prop: 있으면 `groupId === filter || !groupId`만
  렌더 (VIP 그룹별 뷰), 없으면 모두 + 그룹 배지 (admin 풀 뷰)
- /schedule/[slug]?group={groupId} 쿼리로 그룹 필터
- Editor에 그룹별 좌측 색띠 (4 토너 순환: pink/sky/violet/amber + Shared=gray)
  + 셀렉트 자체를 colored chip 스타일

### Pending row 상태 + 인라인 Cancel/Save row

addItem이 row 추가 + pendingItemIds에 등록 → 노란 점선 박스 + amber 배경.
이동/삭제 버튼 비노출, 행 하단에 "Draft" 안내 + Cancel + Save row 버튼.
- Save row: title 비어있으면 disabled, 클릭 시 pending 해제
- Cancel: row 제거
- Free time 프리셋은 즉시 커밋 (3 row 일괄 시 일일이 저장 부담 회피)

### Coverage gate

기존: title 있으면 저장 가능. 신규: 모든 caseProducts.variantId가 schedule
items 중 한 row(committed)에 등장해야 활성화.

- amber 박스로 누락 product 리스트 노출 (`Partner · Product · Variant`)
- pending row가 있으면 별도 안내 + 비활성화
- hover title로 사유

(다음 세션에서 (groupId, variantId) 짝 단위 coverage로 더 정확하게 + picker를
row의 그룹으로 자동 좁히기 + Subpackage Shared 처리 — 이건 설계 토론 중)

---

## 9. agent_notes — agent → admin 메모

[`sql/2026-05-06_cases_agent_notes.sql`]: `cases.agent_notes TEXT`.

쓰는 곳:
- Agent home review page: Send Quote 직전 textarea "Notes for Tiktak"
  (case insert 시 함께 저장)
- Agent 케이스 detail: Trip Setup 아래 별도 박스 (+Add note / Edit / Save /
  Cancel 토글)

읽는 곳:
- Admin 케이스 detail의 ScheduleEditor 섹션 헤더 바로 아래 빨강 테두리
  read-only 박스

박스 톤은 양쪽 모두 흰 배경 + 빨강 두꺼운 테두리(`border-2 border-red-300`)로
일관 — 다른 회색/amber 박스들 사이에서 시선 끌리도록.

---

## 10. 부수 — 잡 폴리시

- **agent cases 페이지에서 0건 status 섹션도 노출** (이전 라운드 룰 재확인)
- **모든 시간 입력 24h 강제** (Time24Input)
- **3자 계약 사인 시 mode별 컬러 분리** + 모드 전환 시 viewer state 리셋 (key prop)
- **Independent Agent 라벨 제거** (QuoteDocument From 라인)
- **편집 중 데이터 정합성** — saveChanges에 recalcDocumentTotal 추가
- **datetime 픽커 → 24h Time input** 통일

---

## 데이터 정합성 점검 발견

**Variants 매핑 오류** — Rarelee `Beauty · Private Consulting` (#P-038)에
`Allergan` variant가 attached 되어 있음. Allergan은 다른 Beauty 시술의 브랜드
variant인데 Rarelee 컨설팅 product에 잘못 박혀 있음. v19 마스터 빌드 시 검수
+ 잘못된 variant_label은 NULL(default sole)로 정리하거나 row 제거 todo 등록
([PROGRESS.md](notes/PROGRESS.md) 컨텐츠 todo).

기존 케이스에 박힌 `variant_label_snapshot`은 immutable이라 신규 견적부터
깨끗해짐.

---

## 코드 변경 요약

### 신규 파일

**SQL**:
- `sql/2026-05-06_case_contracts_evidence.sql`
- `sql/2026-05-06_realtime_case_tables.sql`
- `sql/2026-05-06_document_items_origin_softdelete.sql`
- `sql/2026-05-06_category_sort_order.sql`
- `sql/2026-05-06_cases_agent_notes.sql`

**API**:
- `src/app/api/case-contracts/sign-agent/route.ts`
- `src/app/api/case-contracts/sign-client/route.ts`
- `src/app/api/case-contracts/sign-admin/route.ts`

**컴포넌트/훅**:
- `src/components/Time24Input.tsx`
- `src/hooks/useCaseRealtime.ts`

**문서**:
- `notes/26.05.06.md`

### 수정

- `src/types/schedule.ts` — endBlock/endTime/partner/internalNotes/groupId 추가
- `src/lib/documents.ts` — origin/removed_at, soft delete, activeItems 헬퍼
- `src/lib/caseContracts.ts` — type evidence 컬럼, 직접-write 헬퍼 제거
- `src/components/CaseContractViewer.tsx` — typed_name + identity confirm + 모드별 컬러
- `src/components/AdminCaseContractSection.tsx` — 순서 무관 + agent 알림 + Save PDF
- `src/components/AgentCaseContractSection.tsx` — 신규 API 사용 + Save PDF
- `src/components/CaseDocumentsSection.tsx` — 통화 분기 + deposit 라인 제거 + 버튼 통일
- `src/components/QuoteDocument.tsx` — Independent Agent 제거 + removed_at 필터
- `src/components/SelectedProductsSection.tsx` — activeItems 필터
- `src/components/ScheduleDocument.tsx` — partner eyebrow + group 배지 + filter + showInternalNotes
- `src/components/admin/ScheduleEditor.tsx` — Free time + block range + Time24Input + partner + internalNotes + group + pending + coverage gate (대수술)
- `src/components/DateTime24Picker.tsx` — Time24Input 사용
- `src/app/admin/cases/[id]/page.tsx` — 회계 cyan, partner payouts schedule confirmed 게이트, Edit Selected Products audit trail UI 전면 개편, agent_notes 박스, schedule editor caseGroups + internal preview link
- `src/app/admin/cases/page.tsx` — removed_at 필터 + 빈 상태 표 유지
- `src/app/admin/overview/page.tsx` — 캐시베이시스 회계 모델
- `src/app/admin/settlement/page.tsx` — removed_at 필터
- `src/app/agent/cases/[id]/page.tsx` — agent_notes 편집, removed_at 필터
- `src/app/agent/cases/page.tsx` — 누락 DisplayGroup 추가, 헤더 라벨, 빈 상태 유지
- `src/app/agent/home/review/page.tsx` — agent_notes textarea
- `src/app/case-contract/[token]/page.tsx` — print 모드, 신규 API 사용
- `src/app/schedule/[slug]/page.tsx` — items 컬럼 select, internal/group 쿼리

---

## 정성적 메모

- 5/4 NDA evidentiary 강화 작업의 mirror가 자연스럽게 들어옴 — 같은 패턴 재사용
  하니 코드 작성 + 검수 부담 적음. 5/4 결정이 연쇄 효과 큼.
- Realtime은 작업 양 작은데 사용성 가치 큰 변경. SQL publication 등록 한 줄 +
  훅 하나 + 두 페이지에서 호출. ROI 좋은 자리.
- 캐시베이시스 회계 — accrual보다 의미적으로 정확. "받은 돈만 revenue"가
  비즈니스 직관 그대로. 다음 라운드에서 admin이 실제 수치 보고 익숙해질지
  확인 필요.
- ScheduleEditor 대수술이 오늘의 가장 큰 변경. 단일 컴포넌트에 partner /
  block range / time range / group / Free time / pending / coverage gate /
  internal note까지 다 들어가서 row 한 개의 책임이 많아짐. v2 마무리 후
  필요 시 sub-component 분리 검토.
- group 분리는 단일 schedule + URL 필터 모델로 갔는데, 다음 라운드에서
  picker per-group 필터 + (group, variant) coverage로 더 정확해질 예정.
  현재는 variant 단위 coverage라 "두 그룹이 같은 시술"인 케이스 부정확.
- 데이터 정합성 (variants 매핑 오류) — 시뮬 들어가기 전 데이터 정리 라운드
  에서 잡힐 todo. 코드는 immutable snapshot 모델이라 영향 없음.
- 5/8 D-2. 남은 컨텐츠 todo (계약서 4종, 설문 12문항, 호텔 v19 빌드 cutoff,
  variants 정리)는 사용자 본인 작업.

---

## 11. ScheduleEditor 2차 개선 (두 번째 세션)

### Draft Save

`onSaveDraft?: (items: ScheduleItem[]) => void` prop 추가. Schedule Editor 내부에서
row를 "Save row" 할 때마다 자동 호출 + 글로벌 "Save Draft" 버튼 추가. 전역 publish
(Save Schedule) 성공 시 `onSaveDraft([])` 호출해 draft 초기화.

- `savingDraft` / `draftSaved` 상태로 "Draft saved" 2.5초 토스트 구현
- admin/cases/[id]/page.tsx: `handleSaveDraft` → supabase UPDATE items

### Edit 버튼 (커밋된 row 재수정)

커밋 완료된 row에 연필 아이콘 버튼. 클릭 시:
1. 현재 item 상태를 `editSnapshots: Map<string, ScheduleItem>` 에 스냅샷 저장
2. `pendingItemIds`에 다시 추가 → 노란 점선 편집 모드로 전환
3. 모든 input 활성화

Cancel 버튼:
- `editSnapshots`에 스냅샷이 있으면 → 스냅샷으로 restore + pendingItemIds 제거
- 스냅샷 없으면 (신규 빈 row) → 즉시 제거
- 신규 row인데 데이터가 입력된 경우 → `window.confirm()` 후 제거

이전 버그: Cancel이 무조건 row 삭제 → 스냅샷 restore 패턴으로 수정.

### 필드 잠금 (edit 버튼 클릭 전까지 read-only)

커밋된 row(`!isPending`)의 모든 input/select에 `disabled={!isPending}` 적용.
이전엔 edit 누르지 않아도 필드 직접 편집 가능했음.

### 그룹 재선택 (—Choose group— 재활성화)

HTML `<select>`의 disabled placeholder는 한번 벗어나면 돌아올 수 없음.
해결: sentinel 값 `__choose__` 사용.
- value 계산: `isGroupUnset ? '__choose__' : (item.groupId ?? '')`
- `onChange`: `v === '__choose__'` → `unsetGroupItemIds`에 재추가 + `onApplyVariant(null)` 호출해 제품 정보 초기화
- `isGroupUnset` 상태에선 picker 상품 목록 hidden (`return []`)

이전 버그: "—Choose group—" 선택 시 제품 정보가 남아있었음 + 최초 로드 시 일부 상품만 보이던 이슈 (groupId가 undefined라 scope 조건 `=== null` 불통).

### 삭제 확인 다이얼로그 분기

| 상황 | 동작 |
|---|---|
| 커밋된 row의 X 버튼 | `window.confirm('Remove this item from the schedule?')` |
| 신규 row (빈 상태) X 버튼 | 즉시 삭제 |
| 신규 row (데이터 입력) X 버튼 | confirm 후 삭제 |
| Edit 중 Cancel | 스냅샷 restore (삭제 아님) |

### 상품 중복 선택 허용 + ✓ 표시

이전: 커밋된 variant는 picker에서 숨김 → "2day" 시술 같이 여러 날에 추가해야 하는
케이스 불가.

신규: 숨기지 않고 모두 노출. 대신 이미 커밋된 variant에는 `✓ ` prefix 표시.
`committedVariantContexts: Map<string, Set<string|null>>` — `variantId → Set<groupId>` 맵.

### 소요시간 표시

Picker 옵션 라벨 오른쪽에 `(2day)` / `(3hr)` 형식으로 duration 표시.
- `caseProducts`에 `durationValue`, `durationUnit` 필드 추가
- admin/cases/[id]/page.tsx의 caseProducts 빌드 시 `it.products?.duration_value / duration_unit` 포함

### Shared 색상 — 흰 배경 + 검정 테두리

이전: Shared = gray stripe (비활성화 느낌). 검정으로 교체 시도 → select 드롭다운 배경이 검정으로 오염됨 (CSS 상속).

해결: `chip: 'bg-white border-gray-800'`, `chipText: 'text-gray-900'`.
좌측 색띠(`stripe`)만 `border-l-gray-900` 으로 검게, 배경은 흰색 유지.

### variantTag 필드 분리

`ScheduleItem.variantTag?: string | null` 추가.
- Editor `applyVariantPick`: `title = productName`, `variantTag = variantLabel`
- Editor 행에 chip 형태로 variantTag 노출 (input 옆 회색 pill)
- ScheduleDocument에서 `(text-[10px] bg-gray-100 border border-gray-200 rounded-full)` chip으로 serif title 옆 표시
- 하위 호환: variantTag 없는 구형 item은 title 그대로 표시

---

## 12. ScheduleDocument 전면 재설계

이전: Day 헤더 + block 섹션 단순 나열, cover 없음, group 배지만.
신규: 커버 페이지 + 그룹 분리 day 섹션 + variantTag chip.

### 새 props

```ts
outboundFlight?: FlightData | null
inboundFlight?: FlightData | null
groups?: { id: string; name: string; members: string[] }[]
```

`FlightData`:
```ts
{ departure_airport, arrival_airport, departure_datetime, arrival_datetime, flight_number }
```

### Cover 페이지

1. **그룹 섹션** — 각 그룹을 GROUP_PALETTE 색상 (blue/emerald/orange/purple)의 왼쪽 테두리 카드로 표시.
   그룹 이름 + 소속 멤버 이름 나열.
2. **항공편 섹션** — outbound(출발) + return(귀국) 각각:
   - 출발/도착 공항 코드
   - `formatFlightDateTime()` 으로 날짜 + 시간 파싱
   - 편명
3. Arrival / Departure / Stay 정보 (기존 유지)

### Day 섹션 그룹 분리

`isMultiGroup = groups.length > 1` 일 때:
- Shared 항목 먼저 렌더 (groupId === null)
- 그룹별 섹션 — divider 선 + 그룹 이름 + 색상 왼쪽 테두리

`DayItems` 서브 컴포넌트로 추출해 재사용.

### GROUP_PALETTE (inline styles)

Tailwind 동적 클래스는 print CSS에서 불안정. inline style로 직접 지정:
```ts
const GROUP_PALETTE = [
  { border: '#2563eb', bg: '#eff6ff', text: '#1d4ed8' },   // blue
  { border: '#059669', bg: '#ecfdf5', text: '#047857' },   // emerald
  { border: '#ea580c', bg: '#fff7ed', text: '#c2410c' },   // orange
  { border: '#7c3aed', bg: '#f5f3ff', text: '#6d28d9' },   // purple
]
```

---

## 13. schedule/[slug]/page.tsx 데이터 확장

```sql
-- 추가된 select 필드
cases(
  outbound_flight, inbound_flight,
  case_members(id, ...),
  documents(
    document_groups(
      document_group_members(id, case_member_id),
      ...
    )
  )
)
```

- `FlightData` 타입 파일 내 정의 (CaseLite 바깥)
- `CaseLite.case_members`에 `id` 추가
- `groups` 빌드: quotation document_groups → document_group_members → case_members 조인으로 그룹별 멤버 이름 배열 생성
- `outboundFlight`, `inboundFlight`, `groups` → `ScheduleDocument` 전달

---

## 미완료 — 다음 세션 우선순위

### 1. 스케줄 시간 정렬 버그 (🔥)

렌더된 스케줄에서 시간 순서가 뒤죽박죽 (사용자 스크린샷 + PDF 확인).
`compareScheduleItems`는 맞는데, ScheduleDocument 렌더 시점에서 `groupItemsByDay`
적용 후 `groupDayByBlock` 내부에서 재정렬이 안 되는 것으로 추정.
- `DayItems` 컴포넌트 안에서 items가 이미 정렬된 상태로 들어오는지 확인
- block 내에서 time 기준 정렬 재확인

### 2. Internal notes 스타일 (amber 박스 너무 시각적으로 강함)

`showInternalNotes=true` 모드에서 amber 박스가 너무 도드라짐.
사용자 요청: "굳이 저렇게 안해도 될 것 같아" — VIP 문서 톤에 맞게 더 subtle하게.
방향: amber 배경 제거, 작은 회색 italic 텍스트 형식, 또는 collapsed 토글.
