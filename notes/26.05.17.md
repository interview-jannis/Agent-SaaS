# 26.05.17 — 런칭 전 UX 잔손질 + 계약서 SQL + 치과/Subpackage 노트

## 한 줄 요약
시뮬 D+6, 런칭 직전 라운드. Client 입력 UX (B안: 빈 필수 필드 amber, passport 재입력), 모바일 헤더 버튼 너비 통일, 계약서 3종 SQL 토큰 정리, 치과 per-tooth 가격 + Subpackage agent_note 도입.

코드 커밋은 없음 (5/16 22:03 → 5/18 새벽). 5/17 작업분은 5/18 새벽 커밋에 흡수됨.

---

## 1. Client 등록 phone/email 선택 항목화
- `agent/clients/page.tsx` `handleCreate()` 필수 체크 제거, 라벨 `*` 제거
- 케이스 진행에 필수가 아닌 정보는 등록 단계에서 막지 않음 — 이후 client 상세에서 채우는 흐름

## 2. Client 상세 빈 필수 필드 amber 하이라이트 (B안)
- ViewField: label `*` 포함 + 값 없으면 amber 배경/테두리 래퍼 + amber `—` 대시
- TextInput: `border-amber-300 bg-amber-50` (값 채우면 자동 해제)
- 값 없는 필드만 즉시 식별 가능. 기존 "전체 회색" 대비 작업 우선순위 명확
- 적용: `agent/clients/[id]/page.tsx`

## 3. Passport 번호 입력 + 재입력 확인
- `clientCompleteness.ts`: `passport_number` 필드 추가 (ClientInfo 타입 / getMissingClientFields / CLIENT_INFO_COLUMNS)
- Client 상세: 입력(mono font, amber 하이라이트) + Confirm 필드(불일치 시 red 테두리 + 에러 문구). 저장 시 차단
- View 모드: `Passport No. *` ViewField 노출

## 4. HeroShell 모바일 버튼 너비 통일
- `CaseHeroAction.tsx` children 컨테이너 클래스: `shrink-0 → w-full sm:w-auto [&>button]:flex-1 [&>button]:sm:flex-none`
- 모바일에서 Send Quotation / Send Contract 등 다중 버튼이 가로폭 균등 분할
- 데스크탑은 그대로 (sm 이상에서 `flex-none`)

## 5. 계약서 3종 DB 업데이트 SQL
- NDA / Partnership / Three-Party 본문 verbatim 유지, 서명란 빈칸만 토큰(`{{AGENT_NAME}}` 등)으로 대체
- 파일: `sql/update_contract_templates_260517.sql`
- 본문은 법무 검토 완료된 텍스트를 그대로 유지하고, 동적 치환만 추가 — 추후 본문 수정 없이도 사용자 정보 자동 삽입

## 6. #7 치과 per-tooth 가격
- `products.price_per_tooth` 컬럼 (NULLable). 치과 시술에서 치아 개수에 비례한 견적
- Agent 카탈로그 모달: 치아 개수 −/+ 입력 UI
- Review page: `toothCount → quantity`, `price_per_tooth × toothCount`로 base 계산, variant_label에 `· N teeth` 베이크
- 적용: `agent/product/page.tsx`, `agent/product/review/page.tsx`

## 7. #7 Subpackage agent_note
- `document_items.agent_note` 컬럼 (TEXT NULLable). Agent → Admin 메모 (예: "차량은 5명 가족이라 SUV 필요", "통역은 의료 용어 능숙한 사람으로")
- Agent 카탈로그 모달: 비호텔 Subpackage 서비스 in-cart 시 textarea 노출
- Review page: TripServiceItem/CartItem에 note 저장 후 `addDocumentItem({ agentNote })` 전달
- SelectedProductsSection: amber Note 뱃지로 admin/agent 양쪽 표시

---

## SQL 적용 필요
- `sql/update_contract_templates_260517.sql`
- (이미 적용) `document_items.agent_note`, `products.price_per_tooth`

---

## 5/18 새벽으로 이어진 작업
- 빈 필수 필드 시각 정리는 5/18 새벽 라운드에서 ViewField 양식 통일 (amber 채우기 제거하고 red 테두리만 — 02e5249, e15fd32, 8dc8169)
- 5/17 amber 시그널이 다른 ViewField와 충돌이 커서 5/18 새벽에 red border-only로 일관화
